{
  "framework": null,
  "buildCommand": "mkdir public && touch public/.gitkeep",
  "installCommand": "uv pip install --system -r requirements.txt",
  "outputDirectory": "public",
  "rewrites": [
    {"source": "/(.*)", "destination": "/api/index"}
  ]
}