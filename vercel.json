{
  "framework": null,
  "buildCommand": "mkdir -p public && echo done > public/placeholder.txt",
  "installCommand": "uv pip install --system -r requirements.txt",
  "outputDirectory": "public",
  "devCommand": null,
  "rewrites": [
    {"source": "/(.*)", "destination": "/api/index"}
  ]
}
